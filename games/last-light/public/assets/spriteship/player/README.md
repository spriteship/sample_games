# Lone Blackout Scavenger #2 — Phaser sprite bundle

> Tested against **Phaser 3.90** · Export schema **v1.1**

This sprite bundle was generated by [SpriteShip](https://spriteship.com).
You own the assets — see `LICENSE.txt` for the full terms (TL;DR: use them however you want, no attribution required).

If you just want to verify the export works, double-click **`preview.html`** — it loads the sheet in your browser, no engine setup required.

Every file in this zip is described in `tizo-export.json`; that file's shape is documented in `schema.json` (a standard JSON Schema, also readable by your AI coding assistant).

## Files
- `spritesheet.png` — Packed animation frames (or `spritesheet_<group>.png` for per-direction characters).
- `atlas.json` — TexturePacker JSON Hash atlas + Phaser `animations` block. Pass directly to `load.atlas`.
- `metadata.json` — Raw SpriteShip metadata — frame ranges, splits, mirrors, fps.
- `character.png` — Standalone character image — useful as a portrait or pre-spritesheet fallback.
- `tizo-export.json` — Self-describing manifest. See `schema.json` for its shape.

## Quick start

```js
// Per-direction character — load one atlas per group, then create animations.
import Phaser from 'phaser'

class CharacterScene extends Phaser.Scene {
  preload() {
    this.load.atlas('Lone Blackout Scavenger #2-walk', 'spritesheet_walk.png', 'atlas_walk.json')
  }

  create() {
    const sprite = this.add.sprite(200, 200, 'Lone Blackout Scavenger #2-walk', 'walk_0')
    sprite.anims.play('walk')
  }
}
```


## Animation list
- `walk`

---

## Need help?

- Re-export this asset any time from your SpriteShip dashboard.
- Found a bug or have an integration question? Use the "Report an issue" form in the app.

_Generated by SpriteShip · spriteship.com_
